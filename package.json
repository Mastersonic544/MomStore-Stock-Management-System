{
  "name": "inventory-store-manager",
  "version": "2.0.0",
  "description": "Inventory + point-of-sale web app (Firebase backend, deployed on Vercel)",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "migrate": "node scripts/migrate.mjs"
  },
  "dependencies": {
    "firebase": "^10.12.2",
    "jspdf": "^2.5.1",
    "jspdf-autotable": "^3.8.2"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  }
}
